1. **U-Boot (Universal Boot Loader)**
   - **定义**：类似于 PC 电脑上的 BIOS/UEFI，是开发板通电后最先运行的一段“引导程序”代码。
   - **作用**：负责初始化最基础的硬件（如 CPU、内存、时钟），然后将存放在存储器（eMMC/SD卡）中的操作系统内核（Kernel）加载到内存中并启动它。同时，它还负责提供用于固件烧录的特殊模式（如 Loader 模式、Fastboot 模式等）。
